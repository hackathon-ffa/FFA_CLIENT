export type RowInfoType = {
  text: string;
  width: string;
};

export type ResultListType = {
  name: string;
  department: string | null;
  state: string;
  start: string | null;
  end: string | null;
};
